# TEST VERSION (Sabse simple)
name: Test Build
on: [workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ls -la
      - run: chmod +x gradlew
      - run: ./gradlew assembleDebug
      - run: find . -name "*.apk"
