name: Deploy APK
on: [workflow_dispatch]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Creating APK..."
      - run: mkdir -p output && echo "APK" > output/app.apk
      - uses: actions/upload-artifact@v4
        with:
          name: App
          path: output/app.apk
